const prompt = require("prompt-sync")()

for (let contador = 1; contador <= 10; contador = contador + 1) {
    console.log(`Bem vindo!!!`)
}

// variável = variável + numero
// variavel += numero
// contador = contador + 1 => contador += 1 => contador++
// contador = contador - 1 => contador -= 1 => contador--
// contador = contador * 2 => contador *= 2
// contador = contador / 2 => contador /= 2
